# Agent Setup — EKONT SMART REPORT

> **The authoritative agent guide is `/AGENTS.md` (repo root).**
> This file is kept as a pointer so existing links remain valid.

See the **Setup** section in [`/AGENTS.md`](../../AGENTS.md) for:
- Starting the backend (`just run-backend`)
- Installing the agent CLI (`just install-agent`)
- Logging in (`scada auth login`)
- Per-agent-type configuration (Claude Code, OpenCode, Cursor, Copilot, Windsurf)
